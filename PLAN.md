# Daily Stock Briefing — 계획서 및 제안사항

> 작성일: 2026-05-07  
> 목표: 매일 오전 8:30 KST, 해외 주식 시장 브리핑을 자동으로 생성해 이 레포에 저장

---

## 브리핑 구성 (목표)

1. **글로벌 시장 이슈** — 어제 8:30 ~ 오늘 8:30 KST 사이 주식시장에 영향을 줄 주요 뉴스
2. **섹터별 동향** — 테크/반도체/금융/에너지/헬스케어 대표 기업 이슈
3. **보유 종목 집중 분석** — 뉴스, 공시, 주가 흐름 (현재: QCOM, IONQ, MSFT, OXLC)
4. **고배당주 동향** — 연배당 10%+ 안정적 종목 전반
5. **투자 인사이트** — 그날 참고할 만한 팁 또는 주목 이슈

---

## 핵심 제약: Claude Pro 토큰 한도

| 항목 | 수치 |
|------|------|
| 5시간당 할당 토큰 | ~44,000 토큰 |
| 웹 검색 1회 소모 | ~5,000~15,000 토큰 |
| 원래 계획 (10~15회 검색) | ~100,000~200,000 토큰 ← **불가** |

→ LLM이 웹 검색을 직접 수행하는 방식은 Pro 플랜에서 불가능.

---

## 구현 방안 비교

### 방안 A — 무료 주식 API + 경량 LLM 요약 ✅ (권장)

**구조:**
```
레포 내 Python 스크립트 → 무료 API로 데이터 수집 → LLM이 요약만 담당
```

**흐름:**
1. CCR 에이전트가 레포를 체크아웃
2. `fetch_data.py` 실행 → API에서 데이터 수집 후 JSON 저장
3. LLM이 JSON을 읽고 마크다운 브리핑 생성 (WebSearch 1~2회만 추가)
4. `briefings/YYYY-MM-DD.md` 로 커밋 & 푸시

**예상 토큰:** ~10,000~25,000 토큰/회 → Pro 플랜에서 가능

**활용 가능한 무료 API:**

| API | 제공 데이터 | 무료 한도 |
|-----|------------|-----------|
| [Alpha Vantage](https://www.alphavantage.co/) | 주가, 뉴스, 지표 | 25회/일 |
| Yahoo Finance (비공식) | 주가, 재무, 뉴스 | 제한 없음(비공식) |
| [Finnhub](https://finnhub.io/) | 주가, 뉴스, 공시 | 60회/분 |
| [NewsAPI](https://newsapi.org/) | 뉴스 검색 | 100회/일 (무료) |

**장점:** 토큰 절약, 데이터 품질 안정적, 종목 수정이 config 파일 하나로 가능  
**단점:** API 키 발급 필요 (Alpha Vantage, Finnhub 등), 스크립트 초기 작성 필요

---

### 방안 B — 경량화 웹 검색 (검색 3~4회로 제한)

**구조:**
```
CCR 에이전트가 WebSearch 3~4회만 수행 → 브리핑 생성
```

**예상 토큰:** ~30,000~40,000 토큰/회 → 아슬아슬하게 가능  
**단점:** 검색 범위가 좁아져 브리핑 품질이 낮고, 보유 종목 개별 검색이 어려움

---

### 방안 C — Max 플랜 업그레이드

원래 계획대로 구현 가능. 월 비용 증가를 감수할 경우 선택.

---

## 권장 구현 계획 (방안 A 기준)

### Step 1 — API 키 발급
- [ ] [Alpha Vantage](https://www.alphavantage.co/support/#api-key) 무료 키 발급
- [ ] [Finnhub](https://finnhub.io/register) 무료 키 발급 (뉴스/공시용)
- [ ] 키를 레포 GitHub Secrets에 등록 (직접 노출 금지)

### Step 2 — 데이터 수집 스크립트 작성
- [ ] `scripts/fetch_data.py` — API 호출, 주가·뉴스 수집
- [ ] `portfolio.json` — 보유 종목 목록 (사용자가 직접 수정)

### Step 3 — CCR 루틴 생성
- [ ] Claude Code `/schedule` 로 루틴 등록
- [ ] 실행 시간: 매일 23:30 UTC (= 8:30 KST)
- [ ] 에이전트 프롬프트: 스크립트 실행 → JSON 읽기 → 브리핑 생성 → 커밋

### Step 4 — 테스트 & 검증
- [ ] 수동 실행(Run Now)으로 브리핑 품질 확인
- [ ] 토큰 소모량 실측 후 필요시 조정

---

## 보유 종목 수정 방법

`portfolio.json` 파일을 GitHub 웹에서 직접 수정하면 다음 날 자동 반영됩니다.

```json
{
  "holdings": ["QCOM", "IONQ", "MSFT", "OXLC"],
  "watchlist_criteria": "연배당 10%+ 고배당주, 1-2년 내 부도 위험 없는 종목"
}
```

GitHub → 레포 → `portfolio.json` → 연필 아이콘 클릭 → 편집 → Commit changes

---

## 파일 구조 (예상)

```
daily-stock-briefing/
├── PLAN.md                  # 이 파일 (계획서)
├── portfolio.json           # 보유 종목 설정
├── scripts/
│   └── fetch_data.py        # API 데이터 수집 스크립트
└── briefings/
    ├── 2026-05-07.md        # 날짜별 브리핑
    └── 2026-05-08.md
```

---

## 결정 전 체크리스트

- [ ] 방안 A / B / C 중 어느 방향으로 진행할지 결정
- [ ] 방안 A 선택 시: API 키 발급 의향 있는지 확인
- [ ] GitHub App 설치 완료 여부 확인 (에이전트가 커밋하려면 필수)
- [ ] 브리핑 결과물 확인 방법: GitHub 웹에서 직접 읽기 vs 다른 방법 필요한지
